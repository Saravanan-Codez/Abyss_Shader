# =============================================================================
# Abyss Shader — shaders.txt
# Iris / OptiFine GUI layout.
# Each screen= line lists the options shown on that settings page.
# =============================================================================

# Performance preset selector
profile.Potato=Potato
profile.Medium=Medium
profile.High=High
profile.Ultra=Ultra

# Sliders (continuous drag instead of dropdown)
sliders=VOLUMETRIC_FOG_DENSITY AO_STRENGTH TAA_FEEDBACK BLOOM_THRESHOLD BLOOM_STRENGTH EMISSIVE_BRIGHTNESS CLOUD_COVERAGE CLOUD_SPEED

# ---------------------------------------------------------------------------
# Main menu — shows category buttons only
# ---------------------------------------------------------------------------
screen=LIGHTING_SHADOWS ATMOSPHERICS POST_PROCESSING

# ---------------------------------------------------------------------------
# Lighting & Shadows
# ---------------------------------------------------------------------------
screen.LIGHTING_SHADOWS=SHADOW_MAP_RESOLUTION SHADOW_BLUR EMISSIVE_BRIGHTNESS GODRAY_STEPS VOLUMETRIC_FOG_DENSITY SSAO_SAMPLES AO_STRENGTH

# ---------------------------------------------------------------------------
# Atmospherics — sky, clouds, fog, foliage
# ---------------------------------------------------------------------------
screen.ATMOSPHERICS=WAVING_LEAVES CLOUD_COVERAGE CLOUD_SPEED

# ---------------------------------------------------------------------------
# Post-Processing — AA, bloom, render scale
# ---------------------------------------------------------------------------
screen.POST_PROCESSING=TAA_TOGGLE TAA_FEEDBACK BLOOM BLOOM_THRESHOLD BLOOM_STRENGTH
